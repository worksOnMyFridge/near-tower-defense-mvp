# 🤖 AGENT PROTOCOL — NFTower Defense
> Читать ПОСЛЕ `PROJECT_BIBLE.md`

---

## 🚀 Быстрый старт нового агента

```
[ ] 1. Прочитай PROJECT_BIBLE.md полностью
[ ] 2. Прочитай CHANGELOG.md — раздел "ТЕКУЩИЙ СТАТУС" и последние 5 записей
[ ] 3. Прочитай TASKS.md — найди задачи [~] (в процессе)
[ ] 4. Если есть [~] задачи — уточни у разработчика: "продолжаем X или начинаем Y?"
[ ] 5. Одной фразой подтверди что будешь делать
```

**Не пиши код пока не выполнил checklist.**

---

## 📂 Какие файлы читать и когда

Это ключевое правило для экономии токенов. Загружай только нужное.

| Файл | Когда загружать |
|---|---|
| `PROJECT_BIBLE.md` | **Всегда** — при каждом старте сессии |
| `CHANGELOG.md` | **Всегда** — при каждом старте сессии (последние 5 записей) |
| `TASKS.md` | **Всегда** — при каждом старте сессии |
| `TZ.md` | **Только** когда задача касается: архитектуры, БД, TypeScript-интерфейсов, инфраструктуры |
| `AGENT_PROTOCOL.md` | Только если не знаешь как действовать |

> **Правило трёх файлов:** для большинства задач (написать код, отладить, добавить фичу) достаточно BIBLE + CHANGELOG + TASKS. TZ нужен реже.

---

## 📝 Правила работы

### Перед изменением
1. Укажи что меняешь и зачем
2. Если меняешь существующий файл — покажи что было / что стало
3. Если меняешь **числа** — только через `src/game/constants/gameData.ts`

### После значимого изменения
Добавь запись в CHANGELOG.md:

```markdown
## [YYYY-MM-DD] — [краткое описание]
**Агент:** [имя]
**Изменено:** [список файлов]
**Суть:** [что сделано и почему]
**Следующий шаг:** [конкретное действие]
```

### Если застрял
- Напиши `[?]` вопрос в TASKS.md
- Не выдумывай решение — скажи прямо что неясно
- Лучше спросить, чем сломать SynergySystem

---

## 🔄 Хендофф (передача между агентами)

При завершении сессии обнови HANDOFF в CHANGELOG.md:

```markdown
### HANDOFF — [дата время]
**Сделано за сессию:** [список]
**Статус:** [что работает / что нет]
**Следующий агент:** [конкретное первое действие]
**Открытые вопросы:** [список]
**Критические файлы этой сессии:** [список]
```

---

## 🧠 Контекст по типу задачи

### Задача — написать игровой код
- Читай существующие файлы того же модуля
- TypeScript strict — никаких `any` без комментария
- Числа — только через `gameData.ts`, никогда хардкодом

### Задача — изменить баланс / синергии
- Все правки — в `gameData.ts`
- Перечисли все синергии которые затронет изменение
- Запиши в CHANGELOG

### Задача — БД / Supabase
- Любое изменение схемы = `supabase/migrations/YYYYMMDD_описание.sql`
- RLS обязателен
- Никаких физических удалений — только soft delete

### Задача — NFT / NEAR
- Фазы 1–2: только read-only
- Никогда не храни приватные ключи в коде

---

## 💬 Формат ответа

```
**Что делаю:** [одна фраза]
**Файл:** `путь/к/файлу.ts`

[код]

**Как проверить:** [конкретные шаги]
**Следующий шаг:** [что после этого]
```

При нахождении проблемы:
```
**Проблема:** [описание]
**Где:** [файл:строка]
**Вариант A (быстро):** [решение и почему плохо долгосрочно]
**Вариант B (правильно):** [решение и сколько займёт]
**Рекомендую:** A / B
```

---

## ⛔ Стоп-слова

- **"стоп"** / **"stop"** — прекратить действие
- **"откат"** / **"revert"** — вернуться к последнему рабочему состоянию
- **"не трогай X"** — X заблокирован до явного разрешения

---

## ✅ Работа с TASKS.md

При взятии задачи: `[ ]` → `[~]`, добавь `→ Агент: [имя], [дата]`
При завершении: `[~]` → `[x]`, добавь `→ Готово: [дата]`
При нахождении новой задачи: добавь `[ ]` в нужную фазу, не берись сразу
